@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="wrapper">

    <div class="content" style="width: 100%">
        <div class="container">
            <div class="page-title">
                <h3>
                    Users
                    <a href="@Url.Action("Index","UsersAdmin")" class="btn btn-sm btn-outline-primary float-right"><i class="fas fa-user-shield"></i> Users</a>
                    <a href="@Url.Action("Create")" class="btn btn-sm btn-outline-primary float-right"><i class="fas fa-plus"></i> Create</a>
                </h3>
            </div>
            <div class="box box-primary">
                <div class="box-body">
                    <table width="100%" class="table table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th><b>Name</b></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var role in Model)
                            {
                                <tr>
                                    <td>@role.Name</td>
                                    <td class="text-right">
                                        <a href="@Url.Action("Edit", new { id = role.Id })" class="btn-outline-info btn-rounded"><i class="fas fa-pen"></i></a>
                                        <a href="@Url.Action("Details", new { id = role.Id })" class="btn-outline-danger btn-rounded"><i class="fas fa-info-circle"></i></a>
                                        <a href="@Url.Action("Delete", new { id = role.Id })" class="btn-outline-danger btn-rounded"><i class="fas fa-trash"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

